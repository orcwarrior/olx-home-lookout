import React from "react";
import { StepWrapper } from "@components/SubmitForLookout/StepWrapper";

const Step1EmailAndUrl = (props) => {
  return <StepWrapper {...props}>
    <div>Step 1 email / url</div>
  </StepWrapper>
}

export { Step1EmailAndUrl }

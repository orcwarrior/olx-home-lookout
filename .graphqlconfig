{
  "name": "Hasura Home Lookout GraphQL Schema",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "Default GraphQL Endpoint": {
        "url": "http://localhost:8080/v1beta1/relay",
        "headers": {
          "user-agent": "JS GraphQL",
          "x-hasura-admin-secret": "secret"
        },
        "introspect": false
      }
    }
  }
}

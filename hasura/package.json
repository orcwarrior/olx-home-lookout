{
  "name": "hasura",
  "version": "1.0.0",
  "scripts": {
    "hasura-console": "hasura console",
    "hasura-console:static": "hasura console --static-dir ./console-static",
    "initialize": "npm run metadata:apply && npm run migrate:apply",
    "metadata:apply": "hasura metadata apply",
    "metadata:export": "hasura metadata export",
    "metadata:export:win": "node win-export-metadata.js",
    "migrate:apply": "hasura migrate apply"
  },
  "devDependencies": {
    "hasura-cli": "^1.3.0-beta.4",
    "windows-elevate": "^1.0.1"
  }
}
